This fping.exe is a Cygwin x86_64 build, not a pure native Windows executable.
It depends on cygwin1.dll and any other DLLs listed by cygcheck.
When packaging for NetConsole, keep fping.exe, required DLLs, and license files together.
This distributed bundle is a Cygwin build and must not be represented as a pure native Windows port.
