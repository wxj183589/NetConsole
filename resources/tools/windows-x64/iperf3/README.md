# iPerf3

本目录是版本化的 Windows x64 Cygwin 版 iPerf3：`iperf3.exe`、`cygwin1.dll`、`cygcrypto-3.dll`、`cygz.dll`。四个文件必须位于同一目录；Agent 和桌面端构建时只从本目录复制到交付包内的 `tools/windows-x64/iperf3/`。

当前仓库尚未随本目录补齐可核验的上游许可证和 NOTICE 材料。正式对外分发前必须补齐来源、版本、许可证和 Cygwin 运行依赖记录；构建脚本的文件存在性检查和 IPOP 排除不等同于法律授权确认。材料未齐全前不得新增或替换来源不明二进制。
