# v1.4.2 发布打包交接

- 日期：2026-07-24
- 分支：`main`
- 发布修订：`1861060e`
- 标签：`v1.4.2`
- 推送结果：`github/main`、`nas/main` 及两端 `v1.4.2` 标签均已成功推送。

## 修改内容

- 补充 `v1.4.2` 版本日志与产品内置更新日志，记录现场采集包/采集回传包、稳定局点 UUID、预检冲突、三方合并、恢复快照及不自动执行删除的安全边界。
- 同步提交当前工作区全部待提交源码与交接资料；Feature Profile 原子写入的 `config/profiles/features/.*.lock` 已加入精确忽略规则，不纳入源码。
- 版本构建元数据更新为 `v1.4.2`、`2026-07-24 06:30:36`、源码修订 `8d3b74d9`。

## 验证与产物

- Python 定向测试：`143 passed`；Web 定向测试：`17 passed`；Electron 全量测试：`146 passed`。
- Ruff、`py_compile`、Electron typecheck、Web/Electron production build、PyInstaller 发布门、目录包 smoke 和 NSIS 打包均通过。
- 安装包：`dist/electron/NetConsole-1.4.2-x64-setup.exe`（151,703,148 字节）。
- 目录包：`dist/electron/win-unpacked/NetConsole.exe`。

## 遗留事项

- 尚未执行 Windows 图形界面的人工启动、安装/卸载、升级和签名验收；自动 smoke 不能替代该发布门。
