# Web Composables

本目录存放 Vue 页面复用的轻量响应式组合函数。组合函数只处理视口、组件生命周期和展示状态，不访问数据库、设备连接、Electron 任意能力或 Python 业务规则。

`useAvailablePanelHeight.ts` 根据容器位置和可用视口计算表格、图表面板高度，并在窗口变化时刷新；使用方仍负责设置业务允许的最小高度和底部间距。

修改本目录时运行对应 Vitest；涉及页面布局时同时检查窄窗口、最大化窗口和组件卸载后的监听器清理。
