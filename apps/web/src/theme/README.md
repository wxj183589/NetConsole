# Web 主题与图表语义

本目录定义深浅主题、Design Token、Element Plus 覆盖和 ECharts 配色/语义。它只负责视觉系统，不改变业务状态或数据含义。

主要入口为 `theme.ts`、`tokens.css`、`light.css`、`dark.css` 与 `echarts.ts`。修改颜色、密度或图表状态时运行主题测试并回看 `docs/UI_DESIGN_SYSTEM.md`。
