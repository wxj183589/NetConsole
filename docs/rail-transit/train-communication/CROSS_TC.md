# 跨 TC 检测

跨 TC 状态来自现有车内通信检测结果中的 `cross_tc_ping`，必要时兼容结果中的 `cross_train` 聚合字段。拓扑连线只是展示，不代表检测成功。

后端返回：

- `status`
- `message`
- `updated_at`

点表缺失或不完整时，跨 TC 返回“未配置”；点表完整但没有结果时返回“未检测”；只有结构化检测结果通过或失败时才返回“正常”或“异常”。本页不使用持续 fping/iPerf 替代原车内通信检测。
