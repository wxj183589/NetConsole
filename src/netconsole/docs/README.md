# 包内文档资源

本目录只保存随 `netconsole` Python 包分发的简短版本/变更资料，不是项目级开发规则的替代品。产品状态和版本事实仍以 Core 与仓库 `docs/` 为准。

`changelog.md` 是由项目级 canonical `docs/CHANGELOG.md` 生成的运行时兼容副本，不要手工维护。发布构建会从 canonical 日志生成包内 `netconsole/assets/changelog.md`；纯 Skill、测试或内部整理不伪装成产品发布内容。
