v1.4.3
- 局点数据包导入仅在任务 `COMPLETED` 后重新读取局点清单和数据根，当前设置页及局点数量立即同步；提交、失败和局点列表刷新失败分开提示，并通知 Electron 刷新托盘事实源。
- Electron 托盘使用 Backend Registry 的局点 `display_name`，不再显示 `legacy-*` 等内部 `site_id`；新增快速切换子菜单，重名时才追加内部标识，所有托盘切换请求仍经系统设置的确认、预检、工作区快照、受管 Backend 重启校验与失败恢复。
- 修复 MESH 会话导航：支持既有复合 `session_id`，会话列表“查看”一次进入详情，切换会话时只保留最后一次请求结果。
- 顶部当前局点入口统一接入工作区；设置页恢复后可刷新并定位局点管理。
- 局点切换先做只读预检，随后清理旧局点业务标签、query、缓存和 MESH 图表资源；后端重启校验失败时恢复原工作区快照，不删除局点数据库、原始日志或报告。
- 工作区普通页面默认不缓存且不允许重复；MESH/Online MR 按明确策略缓存或复制。
- 地面无人值守支持首次运行前从基础资料置顶列车，深度采集页显示持久队列位置、当前调度优先级和选择原因；列车详情回填 Ping、活动任务与最近 Session。
- 修正首次 AP 接入切换窗口和超出 AC/Ping 容差的未知位置归类；每日 ZIP 补齐 Ping 汇总与固定目录，流式校验成员哈希，活动 run 立即开始幂等，READY 归档幂等且封账后拒绝同日覆盖，保留期按局点时区计算。
- Windows 安装器将程序目录与业务数据根分离，安装、开发和正式运行统一读取机器级 `DataRoot`；未配置时不再回退 AppData、源码或临时目录。
- 修复全新安装在写入机器级 `DataRoot` 前因日志和路径模块提前初始化而无法校验候选数据根的问题；普通运行仍保持未配置即失败。
- 修复安装器固定探测目标残留导致已有数据根被误判不支持安全重命名；改为根内唯一文件探测、回读和清理，并显示失败步骤与 Windows 错误码。
- 修复全新电脑安装后首次启动可能因 storage manifest 初始化失败而退出：安装阶段在发布机器级指针前初始化或验证 manifest；Backend 启动失败协议改为代码页无关的 ASCII JSON Unicode 转义，其他 Windows 中文环境不再乱码。
- 修复 Windows 空目录与含普通文件目录的矛盾判断：安装器在任何写入探测前先枚举，探测后不再按非空规则复检；打包 Backend 也取消“非空即拒绝”，保留普通文件，仅在 NetConsole 必需路径真实冲突时停止。
- NSIS 安装器显式启用 Unicode，避免其他 Windows 区域设置下中文向导和错误提示乱码。
- 修复尚不存在的 D/E 盘数据目录被 NSIS 内置路径规范化错误拒绝：改用不验证路径存在性的 Win32 `GetFullPathNameW`，规范化不创建目录；错误提示区分 Windows API、NSIS 参数和内部逻辑错误，并新增实际 Unicode NSIS 行为回归。
- 正式安装器改用带 Git short commit 的唯一文件名，并在向导、PE 资源和最终 EXE 内嵌清单中记录外层 Installer 身份；打包前清理旧制品，防止同名旧安装包被误当作新包。
- 新增最终 setup.exe Gate，直接核对 Unicode NSIS、数据根策略源码与新旧文案、Installer/Backend/Frontend commit、生成时间和双次 SHA-256，并输出 release manifest；全新 Windows 实际安装状态在人工验收前保持 `PENDING`。
- 安装器只接受可写、支持原子重命名和 SQLite 写锁的非系统固定磁盘；升级更改数据根使用 staging 校验与原子发布，普通卸载保留业务数据。
- 测试运行仅使用显式 `D:\NetConsoleTestData\<run-id>`；storage manifest、Backend 写锁和 health 诊断补齐统一数据根信息。
- 局点导出拆分为完整迁移包与脱敏分享包：v4 完整包是不要求迁移密码的普通 ZIP，直接包含设备用户名、密码、SNMP community 和隧道凭据；脱敏包继续清除秘密并标记需要重新录入。
- 完整包导入和替换统一使用包内凭据，不再提供凭据策略、`payload.enc` 或敏感 Worker bootstrap；manifest、逐文件 SHA-256、ZIP 与 SQLite 校验失败时不发布半成品，替换失败恢复原局点。
- 设备密码留空时保留，只有显式清除才删除；旧 `username/password` 可兼容读取并在保存时同步正式 SSH 字段。普通 DTO 不回显秘密，本机 Electron 用户点击眼睛后可经受保护接口按字段读取，关闭编辑器即清理本次显示值。

v1.4.2
- 新增独立“轨道交通 / 地面无人值守”：按局点配置运行窗口，复用 AC/基础资料识别正线并维护全车多目标长 Ping、每日深度覆盖队列、2 车/4 MR 资源预算、Session 最终化、每日校验归档和 30 天详细保留；页面关闭或隐藏到托盘不停止后台运行，归档失败保留 active 原始数据。
- 轨旁 AP 基础资料明确区分 AC 当前真实 AP 名称和项目点位编号，FIT-AP 详情按规范化 MAC 唯一联动；新增仅导出、不执行的 AP 重命名命令 TXT Artifact，并对重复 MAC/目标名称和不安全目标做校验。
- AP 点表导入统一归入轨道交通基础资料，新增 AP 交换机端口点表识别、非空字段合并、空里程允许和无效占位跳过；AC 管理移除重复导入入口。
- 新增浏览器式工作区：主窗口与独立工作区窗口都支持标签打开、切换、关闭、固定、复制及“在新窗口打开”；设备详情与带稳定会话 ID 的 MESH 页面按资源身份隔离，普通导航复用已有标签。
- 新增受控窗口布局和标签恢复。恢复数据仅保存规范化内部路由、窗口布局和标签元数据；敏感 query、Token、密码、确认令牌和本机绝对路径会被过滤，损坏布局或多显示器越界安全降级。
- Windows 启动后创建通知区域图标。默认关闭主窗口只隐藏到托盘，Python Backend 和正在运行的业务任务继续运行；托盘菜单可恢复主窗口、新建工作区、打开任务中心，并显示 Backend 与当前局点状态。
- 系统设置新增“关闭主窗口后继续驻留通知区域”开关，托盘菜单与所有窗口同步。只有“退出 NetConsole”执行完整受控退出；托盘不可用时关闭主窗口不隐藏，避免留下无法恢复的后台进程。
- 主窗口顶部新增真实当前局点入口，支持长名称省略、Backend 重启后刷新，并可跳转定位到“系统设置 → 局点与数据管理”；不新增局点状态或顶部切换能力。
- 局点数据包新增现场采集与采集回传类型；跨电脑增量同步以稳定局点 UUID、现场基准、文件 SHA-256 和来源电脑 ID 校验同一局点，不按局点名称或本地自增 ID 猜测关联。
- 回传导入先预检文件、任务、稳定 UUID 记录和字段冲突；文件按哈希追加，任务按稳定任务/事件 ID 合并，设备、FIT-AP 和 AP 记录执行三方字段合并并由用户逐项处理冲突。
- 合并前自动创建数据库恢复快照，失败时恢复主库；删除请求只记录审计而不自动执行，缺少稳定身份的旧记录保持本机数据并明确列为未支持项。
- 正式 Electron 包使用不可编辑的固定生产功能集；功能配置 UI/API 关闭，生产基线缺失或损坏时回退 Registry 稳定默认，系统设置、局点管理、任务中心和日志保持可用。
- 局点切换只被 reconcile 后仍有宿主的活动任务阻止；终态、死 PID 和无宿主残留任务不再永久阻塞，阻塞提示显示具体任务并可打开任务中心，Backend 重启失败会恢复原局点。
- MR-CT 固定表示 CT / 1车厢端，MR-CW 固定表示 CW / 6车厢端；基础资料分别保存端位代码、物理端位、车厢号和站序递增方向头端基准。
- 新增按实际站序方向、编组方向基准和物理端位计算“行驶方向头端 / 行驶方向尾端”的独立模型；折返过渡暂不判定，RSSI 不用于静默交换 CT/CW。
- 当前版本先完成 MR 端位数据模型、基础资料展示和计算规则；完整行程重建、折返识别、双端联合分析与冲突告警仍未接入。
- 基础资料自动区间解锁后可编辑并记录人工覆盖字段，再次生成时默认保留人工调整，也可在草稿内恢复自动值；端点延伸按线路物理端固定节点顺序，正式字段不再保存带站名的端点展示文本。
- 区间改名在同一事务内同步同名轨旁 AP 引用，避免不同线路侧残留旧区间名称。
- 区间物理里程改为依据站台中心里程和端点规则独立生成，上下行共享同一范围；高里程端可保存开放终点，且不再与轨旁 AP 实际里程统计混用。页面、API、metadata、人工覆盖/恢复自动值和模板往返同步支持新字段，旧局点保持兼容。
- 修复轨旁信号图在降采样连续折线上错误显示“当前时刻无有效采样”的问题，仅在真实曲线覆盖区间内吸附最近返回帧，断线和范围外不再显示空提示。
- 轨旁 Tooltip 改为按图表可用高度展示的独立 Vue 浮层，并新增可完整保留当前 frame 全部 ACTIVE/STANDBY 链路的“固定查看”详情面板。
- 同帧、相邻帧、run 重叠和短间隔冲突的轨旁 AP/Radio 序列使用稳定冲突感知配色；曲线、Tooltip、范围列表和固定详情共用颜色。
- Tooltip、固定详情和指针更新不重建图表、缓存或 viewport；软件版本同步升级到 `v1.4.2`。
- MR 原始 MESH 日志分析页离开后保留当前会话、RSSI 图表、时间轴、AP 选择、固定详情和滚动位置；返回时只恢复图表尺寸与轮询，不重复请求或重建已加载图表。
- 车载 MR 收集分析将会话选择、解析、打开本地目录、XLSX 报告、任务窗口和删除统一到顶部操作区；会话表格与选择器同步当前行，并移除底部重复报告卡片。
- Electron 打开会话位置只提交稳定 Session ID，由受管回环后端解析包、raw、会话目录或报告；新增后台会话删除任务，与解析、报告共享资源锁，数据库失败自动恢复隔离目录，且不删除外部源文件或其他会话。
- Windows 正式目录包固定生成 `NetConsole.exe`，package smoke 从 Electron Builder 配置解析名称；冻结后的 `NetConsoleBackend.exe` 无参数运行时显示受管后端提示并返回非零状态，不再启动源码开发链。
- 品牌 ICO 补齐 256px 层，`v1.4.2` 目录包 smoke 与 NSIS 安装包构建通过。

v1.4.1
- AC/FIT-AP 正式开放受控固化与远程登录动作，新增 DPAPI 保护的登录 Profile、公共行菜单和完整 OmniPeek 名称表预览/导出闭环。
- 未保存设备表单连接测试使用一次性内存凭据，并提供分阶段进度、耗时和稳定失败分类；敏感字段不进入任务参数、结果或日志。
- 车内通信检测页改用全部已登记列车，在线状态仅作辅助提示；没有在线快照、数据过期或双端离线时仍可按有效点表启动检测。
- 修复当前会话回落与业务表分页链路，未生成 parsed 数据库时仍可继续查看原始日志和已结构化业务区。
- 配置采集支持勾选恰好两条快照直接对比；轨旁 AP 光衰任务区分调度终态与业务成功、失败和跳过结果。
- MESH 链路明细增加兼容旧库的性能索引和 keyset 分页，保持原有主备链、Radio 和字段语义。
- 软件版本同步升级到 `v1.4.1`，Electron 打包版本与发布入口保持一致。

v1.4.0
- MESH 原始日志分析统一使用 MeshLinkAnalyzer，报告和链路明细导出共享局点/来源/临时参数快照；链路明细导出增加参数配置和“分析参数”Sheet。
- 报告与来源列表只允许删除 outputs 派生文件，原始导入日志、parsed SQLite 和 catalog 保留。

v1.3.9
- Electron 第二波 Qt 功能对等整改完成集成：系统设置、中央功能 profile、网络工具、命令说明、应用日志与安全维护统一进入 Vue + FastAPI + Application Service 正式链路。
- 统一任务窗口、Artifact 和 Desktop Bridge 收口取消、终态、脱敏、下载、打开文件/目录和子窗口恢复；Electron 开发/生产资源启动及无效 Python 失败冒烟通过。
- Qt 从本版本发布门退出，历史行为仅通过 Git 与最终迁移矩阵追溯；无线勘测、SNMP Center 和通用 MIB/OID 平台正式删除，设备管理仅保留 SNMP v1/v2c 只读基础识别，真实设备与桌面人工验收项保持待验收状态。

v1.3.8
- 轨旁 AP 业务：修复详情联表数据含嵌套字典时的文本提取异常，避免归一化展示值触发 `TypeError`。
- Agent 发布：fping/iPerf3 统一从 `resources/tools/windows-x64/` 取用，交付包仅复制 Agent 所需工具；IPOP 不属于 Agent 运行依赖。
- 文件导入：新增统一 XLSX/CSV/JSON/ZIP 文件契约与强校验，错误模块、版本、字段、空文件、损坏文件和不安全 ZIP 在写入前拒绝。
- 网络工具：移除本地网卡配置和工具箱“本机路由”入口；IPOP v4.1 改为用户配置的可选外部工具，正式发布包不携带其二进制。
- UI：标题统一增加 `by WXJ`，修复弹出模块归属；三处业务表格统一全局交互，日志中心首次进入异步加载，Online MR 移除独立设备列表收起按钮。
- 发布：功能开关配置仅源码开发态可见，新增可持久化工程师打包选项及 engineer edition。
- 架构：建立统一 Job Center、Worker Process 与 Export Process，后台任务使用结构化 JSONL 事件、进度、取消和单一终态。
- 车载 MR：实时 SSH 采集、解析和报告分别收敛到长运行 Job、后台解析 Job 与 Export Process；完善启动确认、Ping/iPerf 参数、Busy 解析和状态展示。
- SNMP 中心：GET、GETNEXT、GETBULK、WALK、SET 和多设备批量采集进入统一后台任务，保留 MIB、Trap、Poll 和 H3C 映射现有边界。
- AC 管理：资源刷新、FIT-AP 光衰和安全命令动作进入 AC Domain Service 与 Job Center，保留既有 H3C CLI、解析和业务判断规则。
- AP Identity：完成只读 shadow/diagnostics 与安全摘要评估，默认不展示且不接管生产匹配；可见 UI 阶段继续暂停。
- 稳定性：修复配置采集数据库路径和快照清理，优化设备批量采集进度，并删除架构迁移后确认无引用的旧 Worker/helper。

v1.3.7
- 日志中心：APP 运行日志事件、详情和级别改为中文展示，保留原始事件码和原始详情，支持中文/原始详情复制与中文字段导出。
- 日志中心：分页查询改为按条件读取当前页，默认只加载最近记录，避免大量 APP 日志一次性进入表格造成卡顿。
- 自动清理：启动后后台清理超过 3 天的软件运行日志、日志中心记录和软件缓存，清理范围限定在 APP 日志/缓存白名单内，不触碰采集数据、MR 原始日志、报告和导入文件。
- AC管理：AP 扩展信息补充通用归属模型，支持归属类型、归属站点、归属区间、区间起终点、场段和区域字段，信号 A/B 网布点表可按 AP 名称和 MAC 兜底导入区间归属。
- AC管理：FIT-AP 资源、扩展信息导入预览、导出和搜索补充归属区间字段，避免将区间 AP 强行归属到单一站点。
- 车载 MR 在线采集：无线状态改为默认采集项，不再通过额外勾选控制。
- 车载 MR 收集分析：重构在线诊断解析结果的数据模型，按真实记录时间展示信道繁忙度、接口速率、主链路切换和图表数据，旧 online_diagnosis.sqlite 不再兼容，重新解析后使用新结构。
- 车载 MR 收集分析：优化主链路切换、信道繁忙度、接口 PPS、打流和 RSSI 图表展示，tooltip 改为单个轻量 hover 提示，切换图表或重绘后自动清理旧提示。

v1.3.6
- SNMP 中心：新增 H3C Comware MIB 基线包、产品 MIB 参考表、产品参考版本匹配、产品目录树和 MIB 字典集管理能力，支持 V5 与 V7/V9 多版本 MIB Module 并存。
- SNMP 中心：优化 MIB 导入依赖解析，按导入批次建立 module_name 到文件路径索引，补齐 .mib/.smi/.sm2/.txt 扫描、标准 MIB 搜索路径、缺失依赖汇总和重新编译流程。
- SNMP 中心：优化 MIB 浏览器，支持通用树与 H3C 产品目录树，清理产品参考表中的模块排序前缀，保留真实 HH3C MIB Module 名称，并改进 OID 树排序与节点属性展示。
- SNMP 中心：修复 MIB 浏览器查询操作分派，Get、GetNext、GetBulk、GetSubtree、Walk、BulkWalk、TableWalk、Set 按当前 Operation 独立执行；GetBulk/Walk 可正确返回多条 H3C AP ID 记录。
- SNMP 中心：增强 SNMP 查询体验，保留顶部 Go 作为统一执行入口，支持取消、最大返回提示、空子树原因提示、完整实例 OID 回退到列 OID 和 Trap/Notification 主动查询提示。
- SNMP 中心：修复进入 SNMP 中心与 MIB 浏览器时的后台加载、SQLite 锁库和 UI 卡顿问题，避免旧异步任务覆盖当前页面状态。
- 设备管理：调整顶部操作栏按钮顺序与入口展示，移除顶部“设备详情 / 刷新 / 字段”按钮，优化表格选择列宽度。
- AC管理：轨旁 AP 规划新增备注字段，支持新增、编辑、导入、导出和持久化；AP 上线情况概览移除导出概览、保存历史快照和查看历史入口。
- AC管理：FIT-AP 子页默认隐藏固化状态、新上线状态、新上线来源和新上线采集时间，并同步调整普通导出字段。
- 轨道交通：车内通信检测列车列表、任务生成和检测进度统一按列车号自然数字升序排序，修复 06 车后置和 10 车排序靠前问题。
- 轨道交通：车内通信检测页面接入全局主题，修复亮色主题下主内容仍为深色、主题切换卡顿和递归刷新问题。
- 轨道交通：轨旁 AP 业务导出新增“当前异常光衰”工作表，紧跟“轨旁AP业务”sheet，保留样式、筛选和冻结表头，并排除无光、未插光模块等非光衰质量异常行。
- MR原始MESH日志分析：报告设置窗口高级阈值数字框禁用鼠标滚轮改值，保留手动输入、箭头按钮、键盘和模板刷新能力。
- 车载 MR 收集分析：分析图表改为动态交互图表，支持 tooltip、点击详情、缩放拖动、图例控制、时间轴联动和无打流数据提示，并新增打流速率图表结构。
- 网络工具：iPerf 带宽测试页面改为服务端 / 客户端左右双栏同时显示，保留独立状态、进程和日志输出。
- 无线扫描：移除 2.4G 信道图和 5G 信道图页签，仅保留扫描结果、扫描历史和原始输出。
- 导航性能：优化 AC管理和轨道交通模块切换响应、子页懒加载、状态保持和旧请求覆盖保护。
- 文档：整理项目概览、开发约定、Codex 工作流、数据目录、模块边界、轨道交通业务规则、构建发布和开发历史文档。

v1.3.5
- 客户版：功能隐藏配置改为随软件内置打包，删除外部 runtime 目录后仍保持 customer profile，不再回退到 dev/full。
- 客户版：新增 Shift+Alt+点击版本号的内部调试解锁入口，口令仅以构建期 PBKDF2 哈希写入，启用后只在本次进程临时切换 full profile，重启自动恢复定制版。
- FeatureGate：补充 session override 刷新链路，临时完整模式启用/关闭后主导航、已打开页面、弹出模块、轨道交通和 AC 管理二级页签会按当前 profile 动态恢复或隐藏。
- 打包：Nuitka/PyInstaller 批处理入口增加客户版内部解锁口令提示；也支持通过 --admin-unlock-password 或 NETCONSOLE_ADMIN_UNLOCK_PASSWORD 非交互传入。
- 轨道交通：修复轨旁 AP 业务导出中 AP 侧有收光值但光告警显示“未知”的问题，统一走 AP 光衰阈值判定。
- AC管理：新增独立“FIT-AP扩展信息”页，建立局点级 AP 扩展信息资料库，支持新增、编辑、删除、搜索、异常检查、车站汇总、标准模板导入导出，以及按 AP MAC 优先、AP 名称弱匹配补充 FIT-AP 资源扩展字段。
- AC管理：新增原始设计/布点表智能识别导入，支持多工作表、多行表头、合并单元格、PIS 左线/右线布点表、AP名称+MAC清单、曲线字段、供电/光缆字段和无 MAC 未绑定点位，导入前必须预览确认。
- 局点管理：顶部用户可见“站点”统一调整为“局点”，新建局点支持线路名称、系统类型、网络域和备注元信息，保留业务数据中的车站/归属站点字段语义。
- 数据库：正式启动流程支持新增表、索引类 schema 自动升级，保留既有数据；仅在缺少基础元数据或结构不完整时提示重建。
- Excel导入：屏蔽 openpyxl 读取 WMF 图片时的已知警告，避免不影响表格数据的图片格式提示污染控制台。

v1.3.4
- 优化外部终端配置、MESH原始日志分析报告设置和车内通信检测显示，修复相关统计与导出问题。

v1.3.3
- 轨道交通 / 轨旁 AP 业务：优化“更新该AP”流程，按目标 AP 身份精确刷新 FIT-AP/AP 侧数据；交换机侧优先收窄到当前业务表或历史 LLDP 定位的关联交换机，找不到时再回退车站/全量范围；AP 离线时仍按最后已知链路刷新交换机侧数据，并在更新完成后弹窗提示目标、离线状态、采集范围和成功/失败/跳过统计。
- 轨道交通 / MR 原始 MESH 日志分析：全量 ACTIVE 图表改为独立窗口，按当前 MR、Radio 和源文件范围从数据库全量查询 ACTIVE 链路数据，不再依赖链路明细分页；增加全量查询与渲染日志，保留图表降采样渲染以控制性能。
- 轨道交通 / MR 原始 MESH 日志分析：优化图表 hover 提示，主链路信息合并为单一区块，RSSI 统一显示为 `MR-AP_RSSI: MR/AP`，备份链路简化为 `AP / 站点 / MR-AP_RSSI MR/AP`，不再显示 radio 口，并继续使用 payload 缓存数据避免 hover 查库。
- 轨道交通 / MR 原始 MESH 日志分析：复杂链路明细导出改为后台线程执行，增加阶段进度、按钮禁用、临时文件替换和失败清理，避免大数据导出阻塞界面。
- 轨道交通 / 轨旁 AP 业务：优化导出表格结构、颜色和光衰处理记录，统一在线/离线 AP 身份匹配和光衰状态来源；修复 FIT-AP 光模块采集命令白名单与正式采集测试覆盖。

v1.3.2
- 轨道交通：新增“车内通信检测系统”，按固定 TC1/TC2 拓扑展示 MR、三层交换机、服务器和 VRRP，支持列车列表切换、实时检测进度、动态路径动画、分层结果表和标准 JSON 诊断输出。
- 车内通信检测：复用在线车载 MR 的 AC mesh-link 解析链路，支持列车号与 CT/CW 端别归一化匹配，避免 AC 已在线但车内通信检测误判离线。
- 车内通信检测：诊断主链路改为 MR SSH 接入后执行 H3C CLI ping，地面到车载 MR、核心侧落地 IP、AC 查询失败仅作为接入辅助信息，不直接判定车内网络故障。
- 车内通信检测：新增 MR 对端 MR 长 ping 检测，TC1-MR 与 TC2-MR 使用 `ping -c 50` 互测；当对端 SW/SRV 可达但对端 MR 不可达时，优先提示对端 MR 车内侧接口、IP、VLAN 或路由异常。
- 车内通信检测：优化 COCC 分组下核心交换机识别，COCC 仅作为设备分组；核心侧辅助检测按“核心交换机 / CORE / Switch”设备身份识别并排除无线控制器，界面不再显示“未配置 COCC 核心交换机”。
- 车内通信点表：表头和映射下拉框中文化，支持全局规则、行级规则、Excel 导入导出和地址映射并应用；新增“全部/all”映射，可将主用地址同时作为车内 IP、落地 IP 和 SSH 地址。
- 车内通信点表：修复 VRRP、SRV 主机位和列车 06 硬编码问题，按列车号生成车内网段与 VRRP 地址，统一备注规范，不保留旧的不一致描述。
- 车内通信检测 UI：优化暗色主题下表格、拓扑节点、进度区、检测日志、按钮禁用态和检测中状态的可读性，支持左侧列车列表收起，结果表列宽可调。

v1.3.1
- 打包：修复 Nuitka / PyInstaller 启动后在 release 目录自动生成 docs、tests、project 的运行时路径问题；统一 APP_ROOT / DATA_ROOT 策略，运行时只创建 data、runtime 和 logs 目录，并增加打包后 EXE 自检防回归。
- 轨道交通：新增“列车在线情况”页面，支持从 AC 在线采集车载 MR Mesh Link、展示列车在线状态、当前车站、TC1/TC2 端状态、实时经过事件和独立历史记录查询。
- 列车在线情况：新增映射表管理、Excel 模板导入导出、30 天历史保留、AP 站点匹配、H3C 射频 MAC 容错匹配和采集间隔 3-300 秒动态调整。
- 列车在线情况：新增线路/列车级在线策略，支持自动/未知、双端在线、单端尾端在线、TC1 固定在线、TC2 固定在线，双活缺端显示异常，单活符合策略时显示在线，避免将单端在线固定误判为故障。
- 列车在线情况：历史事件保存 online_policy、expected_end、direction、status_reason 等判定字段，右侧详情与历史查询同步展示在线策略和判断说明。
- 网络工具：新增本地网卡与路由管理入口，支持按需以管理员权限打开，普通窗口在管理员窗口启动后自动关闭，减少重复弹窗。
- 本地网卡：重构为左侧实时状态、右侧 IP / VLAN 配置编辑的分栏布局，实时显示 IPv4、网关、MAC、链路状态、速率和 VLAN 能力。
- 本地网卡：优化 DHCP、静态 IP、备用 IP 和恢复默认配置流程，DHCP 模式自动清空静态字段，避免残留配置被误写。
- VLAN：新增能力模型，区分数值 VLAN ID 与 Packet Priority & VLAN 开关型属性；不再盲写 `VLAN_ID`，ASIX 等仅支持开关的网卡会禁用 VLAN ID 输入并降级为 0。
- 路由管理：优化路由筛选、排序、编辑表格和配置保存，改用 `route.exe` 写入/删除路由，避免 `New-NetRoute -PolicyStore` 兼容性失败。
- 管理员启动：修复 PowerShell CLIXML 误判、黑色控制台闪现和隐藏 PowerShell 窗口问题，提升 Windows 环境下的打开稳定性。
- 打包：新增 Nuitka 发布脚本和版本读取测试，发布包名称跟随统一版本源。
- 轨道交通：优化轨旁 AP 业务定向更新流程，支持按车站或单 AP 减少重复采集，并完善异步采集与数据库写入测试覆盖。

v1.3.0
- 设备管理：设备主数据切换为主用地址、备用地址、系统名称和归属站点，设备管理作为归属站点 SSOT。
- 设备管理：新增设备 MAC 字段，支持从 H3C `display device manuinfo` 的 MAC_ADDRESS 解析并在设备详情中显示。
- 设备管理：设备模板切换为全新精简模板，移除系统名称、SNMP 字段、旧地址字段和隧道本地端口字段；隧道本地端口改为运行时自动分配。
- 设备管理：用户可见字段“站点/位置”统一更名为“归属站点”，并用于 AP 归属站点联动。
- 连接管理：新增统一连接策略，支持主用地址、备用地址和按需 SSH 隧道，优先保障车载 MR 与文件管理远程访问。
- 文件管理：优化 SFTP 连接流程，支持主用、备用、隧道目标重试；SSH 可登录但 SFTP 未开启时，按厂商适配自动尝试启用 SFTP 后重试连接。
- AC管理：FIT-AP资源支持 AP名称 / AP_MAC 搜索，并支持 AP组、状态筛选；移除暂不需要的批量编辑入口。
- AC管理：新增导出 AP 扩展信息模板，模板仅包含 AP名称、AP_MAC、归属站点、里程、点位说明、上下行，并按 AP_MAC 回写。
- AC管理：FIT-AP资源、FIT-AP光衰和轨旁 AP 更新增加加载动画、进度提示和阶段提示，耗时采集、聚合与数据库写入改为异步执行。
- AC管理：修复离线 AP 识别和离线 AP 光告警显示，离线 AP 光告警统一显示为“离线”。
- 轨道交通：轨旁AP业务新增 Link 链路状态列，显示交换机接口 UP / DOWN，端口类型继续保持 access / trunk / hybrid / unknown。
- 轨道交通：轨旁AP业务隐藏并停止导出室内交换机发光字段，保留室内交换机收光字段。
- 轨道交通：优化轨旁AP业务更新流程，增加更新动画和阶段提示，聚合与数据库写入异步执行。
- 轨道交通：修复 XGE / Ten-GigabitEthernet / Ten-GE 接口名归一化，改善 Ten1/0/2 等接口的 LLDP 和光衰匹配。
- 数据持久化：普通采集不再默认写 RAW 日志，采集结果改为运行时缓存和结构化数据库写入，减少 data 目录污染。
- 数据持久化：MESH日志分析写入数据库后自动压缩归档导入源文件，减少 data 目录占用。
- 数据磁盘管理：新增 data 目录磁盘管理，异步扫描并清理临时缓存、调试数据、历史遗留数据，避免误删数据库、报表、配置备份和用户文件。
- UI：左侧导航栏支持收起 / 展开并记忆状态，导航文字居中显示。
- UI：修复 tooltip 和表头说明中的问号乱码，统一归属站点、Link、端口类型等字段说明。
- UI：优化复选框样式，去除复选框后的多余矩形框。

v1.2.0
- AC管理：修复 AP 上线情况概览，停止依赖空 metadata 作为归属主来源，改为以 FIT-AP 光衰记录中的归属站点匹配在线 AP，并补充容量、未归属样本和匹配诊断日志。
- 轨旁AP业务：导出文件新增与 AC 管理一致的 AP 上线情况概览工作表，并修复 AP 侧无光模块、无光衰数据时的显示、染色和统计口径。
- 无线扫描：新增混合扫描模式，合并 Windows WLAN API 与 netsh 结果，同一 BSSID 只显示一条记录，并同时显示频宽、MIMO、加密方式和认证方式。
- 轨道交通：优化轨旁AP业务加载逻辑，修复进入页面空表、首次点击卡顿、进度文字异常、接口历史窗口嵌入主页和光模块历史被遮挡问题。
- 轨旁AP业务：新增光衰采集更新，复用 FIT-AP 光衰诊断逻辑，隐藏内部连接字段和未本地化状态字段，并同步修复导出字段。
- MESH日志分析：新增分析报告导出，支持主链路切换顺序、来回切换、建链顺序、Peer 生命周期、无 Active、多 Active、RSSI、信道繁忙度、原始事件和解析问题统计。
- MESH日志分析：优化 Peer 趋势窗口，支持 Hover 提示、滚轮缩放、拖拽平移、时间滚动条、锚点居中和长时间数据降采样。
- 车载MR在线收集：新增 Fast Pinger v3 工具发现与版本检测，支持高频 Ping 命令构造、输出解析、汇总解析、Ping 采样持久化和 Active 主链路段聚合。
- 车载MR在线收集：新增 MESH 主链路、信道繁忙度、AP 射频统计、切换历史最新记录和接口速率 repeat 采集命令，并支持 Ctrl+C 停止 repeat。
- 车载MR在线收集：重组页面布局，拆分连接、采集周期、射频参数、高频 Ping、高级参数、实时状态和详情页签，限制输入框宽度并保存表格列宽。
- 在线诊断存储：将在线采集会话统一保存到 rail_transit/online_mr 目录，使用 online_diagnosis.sqlite 保存 Ping、接口速率、终端事件和最新切换历史。
- 网络工具：新增 IPERF 带宽测试与无线扫描功能，无线扫描支持轨旁 AP BSSID 反查、频宽/MIMO 解析、主表导出和扫描源选择保存。
- 启动体验：新增启动预加载和加载动画，删除启动页 Net Tools 副标题，统一 Splash、更新日志、主窗口和关于窗口的版本显示。
- 打包：修复 tools 目录未进入发布包的问题；当时使用的旧路径 `tools/fping_v3/Fping_v3.exe`、`tools/iperf/iperf3.exe` 已在后续版本迁移，并同步更新清理打包白名单。
- 稳定性：完善后台 Worker、取消任务、异常恢复、缓存、列宽保存、多语言显示和全量回归测试覆盖。

v1.1.0
- 配置采集中心：支持运行配置和已保存配置采集、保存配置、快照归档、清洗后差异对比和中英文界面。
- 文件管理：新增类似 WinSCP 的双栏文件管理，支持设备文件下载、本地目录打开、设备名称目录规则和 MESH 日志快速选择。
- 设备管理：新增诊断下载、设备分组、详情入口和 AC 网页打开能力。
- AC管理：修复 HTTPS 端口采集、日志解析、旧站点数据库字段补齐、任务失败提示和默认端口回退逻辑。
- 稳定性：完善批量采集、配置采集、文件采集、顺序下载统计和测试覆盖。

v1.0.0
- 初始版本发布。
- 新增 AC管理模块。
- 新增 FIT-AP 光衰分析。
- 新增 LLDP 邻居解析。
- 新增轨旁AP业务。
