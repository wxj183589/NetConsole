fping v5 Windows/Cygwin test bundle

Run:
  fping.exe -J -l -p 100 -t 100 127.0.0.1

This directory must stay together because fping.exe depends on the bundled Cygwin DLLs.
For realtime JSON reading, use D:\study\fping\scripts\test_fping_json_realtime.py or test_fping_json_target.py.
