# IPOP v4.1 可选外部工具说明

IPOP v4.1 是第三方可选工具，不随 NetConsole 分发。

如需使用，请通过“系统设置 → 外部工具”选择用户自行取得的 `IPOP.EXE`。

也可以由用户自行将 `IPOP.EXE` 放入本目录：

`tools/windows-x64/ipop/IPOP.EXE`

NetConsole 不提供该程序的下载、复制、内嵌或再分发，也不会在构建、安装或更新过程中填充本目录。
